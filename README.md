# ADLight — Корпоративный сайт и Калькулятор (Next.js)

Веб-приложение для производственной компании наружной рекламы в Астане.
Включает в себя лендинг, детальные страницы услуг и сложный инженерный калькулятор стоимости с интеграцией WhatsApp.

## 🛠 Технический Стек

* **Core:** Next.js 14 (App Router), TypeScript, React.
* **Styling:** Tailwind CSS v3 (Custom config).
* **UI Components:** * Radix UI (Slider, Switch primitives).
    * Lucide React (Icons).
* **Animations:** AOS (Animate On Scroll), CSS Transitions.
* **Libs:** `react-fast-marquee` (бегущая строка клиентов).
* **Utils:** `clsx`, `tailwind-merge`.

---

## 🚀 Запуск проекта

1.  **Установка зависимостей:**
    ```bash
    npm install
    # Обязательно проверить наличие react-fast-marquee
    npm install react-fast-marquee
    ```

2.  **Запуск локально:**
    ```bash
    npm run dev
    ```

3.  **Открытие:** Перейдите по адресу `http://localhost:3000`.

---

## 📅 Хронология разработки (Change Log)

### Этап 2: Первый запуск и настройка окружения (21.11.2025)
**Статус:** Успешно
* Открыт проект в VS Code (`code .`).
* Установлена библиотека иконок `lucide-react` (Стабильная версия).
* Запущен локальный сервер разработки (`npm run dev`).
* **Технические детали:** Порт сервера: 3000. Пакетный менеджер: npm.

### Этап 4: Исправление конфликтов версий (Troubleshooting) (21.11.2025)
**Проблема:** Конфликт PostCSS (Next.js установил Beta-версию Tailwind v4).
**Решение:**
* Принудительно удалены пакеты: `tailwindcss`, `postcss`, `autoprefixer`.
* Установлены стабильные LTS версии: `tailwindcss@3.4.17`, `postcss@8.4.35`, `autoprefixer@10.4.17`.
* Очищен кэш Next.js (`.next folder`) и перезапущен сервер.

### Этап 5: Наполнение главной страницы и Интерактивность (21.11.2025)
**Статус:** Успешно
* **Режим Client Component:** Добавлена директива `"use client"` в `page.tsx`.
* **Карусель услуг:** Реализован горизонтальный скролл (snap-x) + перехват колеса мыши. Скрыт нативный скроллбар.
* **FAQ:** Реализован аккордеон через `useState`.
* **Bugfix:** Исправлен JSX-синтаксис для спецсимволов.

### Этап 6: Адаптивность и Навигация (21.11.2025)
**Статус:** Успешно
* **Header:** Создан клиентский компонент с мобильным меню (шторка).
* **Layout:** Хедер интегрирован глобально.
* **Result:** Меню полностью адаптивно на мобильных устройствах.

### Этап 7: Калькулятор v1.0 и Страница Услуги (21.11.2025)
**Статус:** Успешно
* **Calculator.tsx:** Логика расчета стоимости (useState, Range sliders).
* **Страница услуги:** `/services/volume-letters` (SEO, характеристики, интеграция калькулятора).
* **Навигация:** Реализованы хлебные крошки.

### Этап 8: Финализация Главной страницы (UI/UX) (21.11.2025)
**Статус:** Успешно
* **Hero Section:** Редизайн (сетка 2x2, выравнивание).
* **Marquee:** Бегущая строка логотипов клиентов (`react-fast-marquee`) с эффектом grayscale->color.
* **Smart Carousel:** Гибридное управление (Drag & Drop + Wheel). Удалены конфликтующие классы `snap-x`.
* **Bento Grid:** Сетка портфолио с темным фоном.

### Этап 9: Финальная сборка Главной страницы (21.11.2025)
**Статус:** Production Ready
* **Trust Marquee:** Добавлены градиентные шторки.
* **Process & Tech:** Блоки с этапами работы и технологиями (Oracal, Flame, UV).
* **Map & Contacts:** Темная Яндекс.Карта (CSS Filter Invert) + плавающая карточка контактов.
* **Техническое:** `useRef` для манипуляций с DOM.

### Этап 10: Финальная полировка и Анимация (22.11.2025)
**Статус:** Успешно
* **AOS Integration:** Подключен `Animate On Scroll` (плавное появление секций).
* **Bugfix Marquee:** Исправлено наслоение текста, увеличен размер логотипов.
* **Social Proof:** Добавлены отзывы (стиль 2GIS).

### Этап 11: Интеграция Реального Контента и Типизация (22.11.2025)
**Статус:** Успешно (Stable)
* **TypeScript Fix:** Исправлена типизация `ref` в скроллерах.
* **Reviews Carousel:** Полная логика управления (Drag/Wheel/Btn). Адаптив: 1 карточка (Mobile) / 3 карточки (Desktop).
* **Content:** Заменены заглушки Unsplash на реальные фото производства ADLight (папка `/public`).
* **Дизайн-код:** Добавлена иллюстрация "Одобренного эскиза".

### Этап 12: Рефакторинг Калькулятора v2.0 и Связность (23.11.2025)
**Статус:** Успешно
* **Новая логика расчета:**
    * Отдельные поля для "Названия" и "Деятельности".
    * Подсчет символов без пробелов.
    * Выбор высоты монтажа (До 5м / Свыше 5м - автовышка).
* **UI/UX Калькулятора:**
    * **Desktop:** Липкий блок (Sticky) с ценой справа.
    * **Mobile:** Фиксированная нижняя панель (Bottom Bar).
    * Визуализация: Превью-картинки типов букв с градиентом и текстом.
* **Продающие блоки:** "Что входит в стоимость" и "FAQ" прямо на странице калькулятора.
* **Landing Page Услуги:** Создана страница `/services/volume-letters/` со сравнением технологий и перелинковкой.
* **Bugfix:** Устранен горизонтальный скролл на мобильных (`overflow-x: hidden`). Исправлена работа AOS при навигации (`usePathname`).

### Этап 13: Компонентная Архитектура и Рефакторинг (23.11.2025)
**Статус:** Успешно (Scalable Architecture)

**Глобальный рефакторинг кода:**
Код главной страницы (`app/page.tsx`) был разбит на независимые модули. Теперь `page.tsx` содержит только импорты и структуру, что облегчает поддержку.

**Создана библиотека компонентов (`/components`):**
* `ClientsMarquee.tsx` — Бегущая строка логотипов клиентов.
* `DesignCodeBlock.tsx` — Блок про штрафы и дизайн-код (переиспользуется на услугах).
* `CallToAction.tsx` — Лид-магнит (Форма заявки). Принимает пропс `source` для отслеживания источника лида в WhatsApp.
* `ProjectsBento.tsx` — Сетка портфолио. Принимает `title` и `subtitle`.
* `ReviewsCarousel.tsx` — Слайдер отзывов с логикой Drag & Drop.
* `ServicesCarousel.tsx` — Карусель услуг.
    * *Feature:* Добавлен пропс `hiddenLink` для скрытия текущей услуги из списка (чтобы не ссылаться на саму себя).
* `ServicesCrossSell.tsx` — Альтернативный блок перелинковки (статичные карточки).

**Страница "Объемные буквы":**
* Полностью переверстана с использованием новых компонентов.
* Реализован `Sticky Header` (липкая шапка) с кнопкой "Назад".
* Исправлена адаптивность блока "Виды букв" для мобильных устройств.

**Bugfix (Global):**
* Устранены проблемы с двойным скроллом и горизонтальной прокруткой.
* Оптимизированы отступы (`py-12` mobile / `py-24` desktop).

### Этап 14: Система Портфолио и Кейсов (25.11.2025)
**Статус:** Успешно (Dynamic Routing & Data)

**Архитектура данных:**
* Создан файл-имитация базы данных `lib/projectsData.ts`.
* Реализована структура проекта: `slug`, `categories` (мультитеги), `specs`, `gallery`, `videoUrl`.

**Страница Портфолио (`/portfolio`):**
* Реализован `Client Component` с фильтрацией проектов по категориям (useState).
* Сортировка проектов по дате.
* Карточки с "живыми" эффектами наведения (Zoom, теги внутри фото).

**Страница Кейса (`/portfolio/[slug]`):**
* **Dynamic Routing:** Одна страница-шаблон подстраивается под любой проект.
* **Hero:** Полноэкранное фото с затемнением и мета-данными.
* **Video:** Интеграция YouTube-плеера (опционально).
* **Navigation:** Умная кнопка "Следующий проект" в виде большого баннера с предпросмотром.
* **Gallery:** Реализован компонент `ImageGallery` с модальным окном (Lightbox) и управлением с клавиатуры.

**Компоненты:**
* `ImageGallery.tsx` — Сетка фото + Lightbox.
* `ComplexCTA.tsx` — Единый продающий блок (Скидка + Калькулятор) для футера страниц.

### Этап 15: Редизайн Услуг и Финализация Портфолио (25.11.2025)
**Статус:** Успешно (UX/UI Polish)

**Страница Услуг (`/services`):**
* **Hero Section:** Внедрено 3D-изображение города (`city.png`) с адаптивным скрытием на мобильных. Добавлены кнопки-якоря.
* **Каталог:** Редизайн карточек — компактный вид (`h-[280px]`), теги-ярлыки, группировка по категориям.
* **CTA:** Внедрен единый блок `ComplexCTA` (Скидка + Калькулятор).
* **Fix:** Убран локальный хедер, используется глобальное меню.

**Страница Кейса (`/portfolio/[slug]`):**
* **Hero:** Полноэкранное фото с градиентом. Хлебные крошки перемещены внутрь секции.
* **Gallery Fix:** Компонент `ImageGallery` переписан с использованием **React Portals**. Теперь лайтбокс гарантированно перекрывает все слои (z-index: 9999) и не ломает Sticky-хедер при закрытии.
* **Tags:** Реализован маппинг ID категорий в русские названия (`letters` -> `Объемные буквы`).
* **Navigation:** Блок "Следующий проект" переделан в стиле Netflix (большой баннер с превью).

**Калькулятор (`/calculator`):**
* **UX:** Добавлена кнопка "Назад" (`router.back()`) вместо хлебных крошек.
* **Fix:** Шаг ползунка высоты изменен с 5 см на 1 см для точности.
* **Layout:** Убран конфликтующий локальный хедер, настроены отступы (`pt-28`).

**Global CSS & Fixes:**
* **Sticky Header:** Исправлен баг с отваливающимся меню. Заменено `overflow-x: hidden` на `overflow-x: clip` в `globals.css`.
* **Scroll:** Внедрен плавный скролл (`scroll-behavior: smooth`) для якорных ссылок.

### Этап 16: Масштабирование контента и Страницы услуг (26.11.2025)
**Статус:** Успешно (Content Expansion)

**Созданы детальные посадочные страницы (Services):**
Для каждого направления разработан уникальный контент с учетом специфики (боли клиента, технические детали):
* `/services/lightboxes` — Световые короба (Акцент на яркость и формы).
* `/services/panel-brackets` — Панель-кронштейны (Акцент на пешеходный трафик).
* `/services/neon` — Неон (Акцент на интерьер и отсутствие согласования).
* `/services/roof-installations` — Крышные установки (Инженерный подход, нагрузки).
* `/services/entrance-groups` — Входные группы (Капитальное строительство, композит).
* `/services/pylons` — Стелы и Пилоны (Фундамент, навигация).
* `/services/navigation` — Таблички (Wayfinding, материалы).

**Обновление компонентов:**
* **ServicesCarousel:** Добавлена логика фильтрации. Компонент теперь принимает пропс `hiddenLink`, чтобы исключать текущую услугу из списка "Что еще мы делаем".
* **Header & Footer:** Обновлены все ссылки, теперь навигация полностью сквозная и рабочая.
* **Calculator:** Удален конфликтующий локальный хедер, добавлена кнопка "Назад" (`router.back()`), возвращены информационные блоки.

**UI/UX Улучшения:**
* **Страница Каталога (`/services`):** Внедрен Hero-блок с 3D-иллюстрацией города и быстрой навигацией по якорям. Карточки услуг переделаны в компактный вид с тегами.
* **Mobile Optimization:** Исправлены отступы на мобильных устройствах, скрыты тяжелые графические элементы (3D город) на малых экранах.
* **Bugfixes:** Устранены ошибки гидратации (`onError` в SSR), исправлен `z-index` для галереи и меню.

### Этап 17: Экспертный контент и Дизайн-код (27.11.2025)
**Статус:** Успешно (Trust & Authority)

**Страница Дизайн-кода (`/design-code`):**
* Разработана посадочная страница-гайд по правилам наружной рекламы в Астане.
* **Контент:** Чек-листы "Разрешено/Запрещено", Инфографика допустимых размеров букв по этажности, Табы с каталогом форматов.
* **Интерактив:** Внедрено модальное окно `ConsultationModal` (React Portal) для захвата лидов на консультацию.
* **Анимация:** Настроена плавная анимация появления модального окна (Fade In + Zoom).

**Улучшение конверсии (CRO):**
* **ComplexCTA:** Разработан и внедрен новый финальный блок для страниц услуг, объединяющий оффер "Комплексное оформление" (скидка 10%) и две точки входа: Калькулятор (для самостоятельных) и Форма заявки (для быстрых).
* **Умная подсветка:** Компонент `CallToAction` теперь умеет автоматически подсвечивать ключевые слова (например, "Дизайн-код") в заголовках, делая оффер заметнее.

**Fixes & Refactoring:**
* Исправлены ссылки в `ServicesCarousel` (убраны заглушки `#`).
* Реализован горизонтальный скролл фильтров на десктопе (mouse wheel support).
* Исправлена проблема перекрытия контента хедером на странице Кейсов (`z-index`, отступы).